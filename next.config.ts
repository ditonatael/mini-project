import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["media-photos.depop.com"], // Add the external image domain here
  },
};

export default nextConfig;
